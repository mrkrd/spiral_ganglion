-*- mode: org -*-

* run_ci.py stim.mat out.mat
This program reads stim.mat Matlab file of electrical stimulation.
The file must contain stimulation matrix (stim) and the sampling
frequency (fs).

Output is stored as a cell of spike trains.  Each spike train
contains an array of spike timings in milliseconds.

* simple_ci_simulator.py
Script showing how to use CI simulator function within python.

* single_neuron.py
Script showing electrical stimulation of a single neuron.
